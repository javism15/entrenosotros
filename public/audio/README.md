# Audios personales

Coloca aquí mensajes de audio compatibles con Safari iOS, por ejemplo M4A o MP3.
Después referencia cada archivo desde `data/gameConfig.ts`, por ejemplo:

`/audio/carla-room-message.m4a`

El juego nunca reproduce audio automáticamente.
